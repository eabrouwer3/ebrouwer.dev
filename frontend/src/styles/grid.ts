import styled from "styled-components";

const Body = styled.div`
  flex: 1;
  max-width: 800px;
`;

const Row = styled.div`
  display: flex;
`;

const Col = styled.div`
  flex: 1;
`;

export {Body, Row, Col};